ingress:
  - hostname: clinicflow.vet
    service: http://gateway:80
  - hostname: demo.clinicflow.vet
    service: http://gateway:80
  - service: http_status:404

# Note: Since we are using a Managed Tunnel (Remote Configuration), 
# these rules should also be configured in the Cloudflare Dashboard 
# under Networks -> Tunnels -> [ClinicFlow] -> Public Hostnames.
